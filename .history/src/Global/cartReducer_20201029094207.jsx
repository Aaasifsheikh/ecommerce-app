export const cartReducer = (state, action) => {
    const {shoppingCart, totarice, qty} = state;
    let product;
    let index;
    let updatedPrice;
    let updatedQty;

  switch (action.type) {
    case "ADD_TO_CART":

  }
};
